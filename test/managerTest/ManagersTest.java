/*package managerTest;

import manager.InMemoryHistoryManager;
import manager.InMemoryTaskManager;
import manager.Managers;
import org.junit.jupiter.api.Test;

import static org.junit.jupiter.api.Assertions.assertInstanceOf;

public class ManagersTest {
    @Test
    void shouldObjectOfInMemoryTaskManagerClassMustBeCreated() {
        assertInstanceOf(InMemoryTaskManager.class, Managers.getDefault(), "Созданный объект не является объектом класса InMemoryTaskManager");
    }

    @Test
    void shouldObjectOfInMemoryHistoryManagerClassMustBeCreated() {
        assertInstanceOf(InMemoryHistoryManager.class, Managers.getDefaultHistory(), "Созданный объект не является объектом класса InMemoryHistoryManager");
    }
}*/
